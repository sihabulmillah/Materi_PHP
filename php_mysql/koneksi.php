<?php
$host = "localhost";
$username = "sihab";
$password = "password";
$db = "db_praktik";

$conn = mysqli_connect($host, $username, $password, $db);

// memerikas koneksi 
// if ($koneksi) {
    // untuk mengetahui letak erornya
    // die("koneksi gagal".mysqli_connect_error()); 

    // echo "berhasil";
// } else {
    // echo "gagal";
// }
    // untuk menutup koneksi
// mysqli_close($koneksi);
